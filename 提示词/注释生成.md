- 为该 TypeScript 文件中的所有变量、类、方法、属性、对象属性、类型、类型属性生成中文注释。请保留原有的英文注释，并在其上方添加中文注释。
- 注释格式要求如下（必须满足如下要求）：
  - 为每个变量、类、方法、属性添加对应的中文注释。
  - 对于方法、函数，必须为每个参数添加注释，格式为：@param 参数名 参数类型及说明。
  - 对于返回值也要添加注释：@returns 返回值类型及说明。
  - 中文注释应以自然、简洁、符合中文阅读习惯的方式进行描述。
  - 保留原有的英文注释，并在中文注释**下方**保留英文注释，避免删除原有内容。
- 注意：请确保注释内容准确无误，避免误导读者。所有的变量、类、方法、属性、对象属性、类型、type、interface都要生成注释

案例1：

- 之前

  ```typescript
  export interface TargetFindOptions {
    /**
     * When true, object detection happens on per-pixel basis rather than on per-bounding-box
     * @type Boolean
     */
    perPixelTargetFind: boolean;
  }
  ```

- 修改后

  ```typescript
  /**
   * 对象检测相关的选项
   */
  export interface TargetFindOptions {
    /**
     * 指示对象检测是否应基于每个像素而不是每个边界框进行
     *
     * When true, object detection happens on per-pixel basis rather than on per-bounding-box
     * @type Boolean
     */
    perPixelTargetFind: boolean;
  }
  ```

案例2：

- 之前

  ```typescript
  export const capValue = (min: number, value: number, max: number) =>
    Math.max(min, Math.min(value, max));
  ```

- 修改后

  ```typescript
  /**
   * 限制数值在指定范围内
   * @param min 最小值
   * @param value 当前值
   * @param max 最大值
   * @returns 限制后的值
   */
  export const capValue = (min: number, value: number, max: number) =>
    Math.max(min, Math.min(value, max));
  ```

案例3:

- 之前：

  ```typescript
  /**
   * For functionalities on keyDown + ctrl || cmd
   */
  export const ctrlKeysMapDown: TKeyMapIText = {
    65: 'cmdAll',
  };
  ```

- 修改后：

  ```typescript
  /**
   * 用于 keyDown + ctrl || cmd 的功能
   *
   * For functionalities on keyDown + ctrl || cmd
   */
  export const ctrlKeysMapDown: TKeyMapIText = {
    /**
     * 全选
     */
    65: 'cmdAll',
  };
  ```

案例4:

- 之前：

  ```typescript
  interface UniqueITextProps {
    selectionStart: number;
  }
  ```

- 修改后：

  ```typescript
  /**
   * IText 独有的属性接口
   */
  interface UniqueITextProps {
    /**
     * 文本选区开始的索引（如果没有选区，则为光标位置）
     */
    selectionStart: number;
  }
  ```

案例5:

- 之前：

  ```typescript
  export type CursorBoundaries = {
    left: number;
  };
  ```

- 修改后：

  ```typescript
  /**
   * 光标边界接口
   */
  export type CursorBoundaries = {
    /**
     * 光标左侧位置
     */
    left: number;
  };
  ```

案例6:

- 之前：

  ```typescript
  const protectedDefaultValues = {
    _selectionDirection: null,
  };
  ```

- 修改后：

  ```typescript
  /**
   * 受保护的默认值
   */
  const protectedDefaultValues = {
    /**
     * 隐藏的文本区域
     */
    _selectionDirection: null,
  };
  ```
